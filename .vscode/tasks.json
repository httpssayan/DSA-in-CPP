{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run with input-output",
            "type": "shell",
            "command": "g++ -std=c++17 ${file} -o ${fileBasenameNoExtension}; .\\${fileBasenameNoExtension} < input.txt > output.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
